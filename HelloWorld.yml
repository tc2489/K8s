---
apiVersion: v1
kind: Pod
metadata:
  name: helloworld-deployment
spec:
  replicas: 2
  template:
  metadata:
    labels:
      app: helloworld
  spec:
    containers:
    - name: k8s-test
      image: hello-world:latest
      ports:
      - name: nodejs-port
        containerPort: 3000
